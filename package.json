{
  "private": true,
  "name": "helix-prod-worker",
  "version": "1.0.0",
  "description": "A template for kick starting a Helix Production Workers project",
  "module": "./dist/index.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "log": "wrangler tail -f pretty",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "",
  "license": "Apache-2.0",
  "devDependencies": {
    "prettier": "2.6.2"
  }
}
